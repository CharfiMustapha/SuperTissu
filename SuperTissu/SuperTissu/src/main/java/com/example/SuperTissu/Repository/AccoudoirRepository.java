package com.example.SuperTissu.Repository;

import com.example.SuperTissu.Entity.Accoudoir;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccoudoirRepository extends JpaRepository<Accoudoir, Long> {
}