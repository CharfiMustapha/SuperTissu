package com.example.SuperTissu.Repository;

import com.example.SuperTissu.Entity.Watt;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WattRepository extends JpaRepository<Watt, Long> {
}